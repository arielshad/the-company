# EXAMPLE ONLY — the encryptedData values below are placeholders, not real
# ciphertext. Generate the real file with `kubeseal` per ../README.md and commit
# it as gateway-sealedsecret.yaml (without the .example suffix). Never commit
# plaintext Secret manifests.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: gateway-secrets
  namespace: companyos-system
spec:
  encryptedData:
    OIDC_CLIENT_SECRET: AgB...PLACEHOLDER_CIPHERTEXT...==
    DATABASE_URL: AgB...PLACEHOLDER_CIPHERTEXT...==
    REDIS_URL: AgB...PLACEHOLDER_CIPHERTEXT...==
  template:
    metadata:
      name: gateway-secrets
      namespace: companyos-system
    type: Opaque
