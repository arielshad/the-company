apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: companyos-platform
commonLabels:
  app.kubernetes.io/part-of: companyos
  app.kubernetes.io/managed-by: argocd
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: openfga-model
    files:
      - model.fga
